import { SalaryDirective } from './salary.directive';

describe('SalaryDirective', () => {
  it('should create an instance', () => {
    const directive = new SalaryDirective();
    expect(directive).toBeTruthy();
  });
});
